interface:
  display_name: "Claude Session Manager"
  short_description: "List and export Claude Code JSONL sessions"
  default_prompt: "Use $claude-session-manager to list, inspect, or export Claude Code session JSONL transcripts as readable Markdown."
